# Core scientific computing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Geospatial
geopandas>=0.10.0
shapely>=1.8.0
fiona>=1.8.0
pyproj>=3.2.0

# Network analysis
networkx>=2.6

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0
plotly>=5.0.0

# Jupyter (if using notebooks)
jupyter>=1.0.0
ipykernel>=6.0.0

# Web development (for data processing to web formats)
geojson>=2.5.0

# QGIS Python API (if running outside QGIS)
# Note: QGIS provides these when running within QGIS
# qgis-core (install via conda or use QGIS's Python)
